node_modules
dist
.DS_Store
*.tsbuildinfo
.env.*
!.env.example
# Nota: .env (sem sufixo) e' rastreado nos repos de cliente (batista-site etc.)
# com credenciais Supabase + identidade da escola. Vite o carrega em TODOS os
# modos (dev, preview, build). Use .env.local para overrides locais (gitignored
# pelo pattern *.local abaixo).
*.local

# Secrets e credenciais (qualquer arquivo de secrets/service-account)
**/secrets*.json
**/service-account*.json
docs/batista_secrets_google.json

# Claude Code — arquivos locais de sessão/ambiente
.claude/
